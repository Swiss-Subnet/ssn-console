version: '2'

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
  exclusions:
    generated: lax
    paths:
      - backend_gen.go

formatters:
  enable:
    - gofumpt
    - goimports
