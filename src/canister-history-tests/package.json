{
  "name": "canister-history-tests",
  "private": true,
  "scripts": {
    "test": "tsc && vitest run"
  },
  "dependencies": {
    "@ssn/canister-history-api": "workspace:*",
    "@ssn/management-canister": "workspace:*"
  }
}
