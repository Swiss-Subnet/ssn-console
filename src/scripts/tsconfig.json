{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "types": ["bun"],
    "baseUrl": "."
  },
  "include": ["src/**/*.ts"],
  "exclude": [".turbo", "dist", "node_modules"]
}
